const Input = (props) => {
    return (
        <div className="inputBox w-full">
            <input className='font-inter inputInput' type='text' {...props} />
        </div>
    )
}

export default Input